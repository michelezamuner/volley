{
    "name": "volley",
    "version": "0.0.0",
    "description": "Volleyball game",
    "main": "main.js",
    "repository": "https://gitlab.com/michelezamuner/volley",
    "author": {
        "name": "Michele Zamuner",
        "email": "michelezamuner@gmail.com"
    },
    "license": "MIT",
    "devDependencies": {
        "jest": "^24.9"
    },
    "scripts": {
        "test": "SC_VLY_ROOT=$(pwd) node --use_strict ./node_modules/.bin/jest",
        "coverage": "SC_VLY_ROOT=$(pwd) node --use_strict ./node_modules/.bin/jest --coverage",
        "ci": "SC_VLY_ROOT=$(pwd) JEST_ENV=ci node --use_strict ./node_modules/.bin/jest --coverage"
    }
}
